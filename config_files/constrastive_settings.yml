experiment:
  gpu: 1
  path_output_rec_performance: external_results/contrastive/{}/

  dataset: movielens
  data_config:
    strategy: fixed
    train_path: ../data/{}/train2id.tsv
    test_path: ../data/{}/test2id_elliot.tsv
  evaluation:
    simple_metrics: [MAP, Precision, MAR, Recall, F1, nDCG, Gini, ItemCoverage, EFD, EPC]
    paired_ttest: False
    wilcoxon_test: False
  models:
    #ProxyRecommender:
    #  file_path: 'umap_pred/fixed_dbbook2/0_drop_04_selfatt_crossatt_top5.tsv'
    RecommendationFolder:
      folder: external_predictions/contrastive/movielens/
  top_k: 5